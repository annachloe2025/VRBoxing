# CLAUDE.md — VRBoxing プロジェクト

**このファイルは Claude（AIアシスタント）が最初に読むファイル。**
プロジェクトに入るたびに、これを読めば文脈を把握できる状態にしておく。

---

## プロジェクト概要

**VRBoxing** — Valve Index 向けの個人開発 VR ボクシング／フィットネスゲーム。

- **目的**:
    1. フィットネスとして続けられる VR ボクシング体験を作る
    2. Unity / VR 開発の学習
    3. **女性キャラクターと対戦できる**（モチベーション維持の核心）
- **参考作品**: Thrill of the Fight（Ian Fitz 開発）

## ユーザーの背景（重要）

- VR デバイス: **Valve Index**（Knuckles コントローラー）を所持。Quest は持っていない
- **性同一性障害**があり、**女性キャラでないとモチベーションが続かない**
    - これは「個人の好み」ではなく「続けるための必須要件」として扱う
    - キャラ選定の会話では他の選択肢（男性キャラ）を提案しない
- **認知負荷が最重要の制約**: ユーザーの作業量＝認知負荷。作業を増やすと脳が疲れて開発が止まる
    - 解決策は「提案」ではなく「自動化」で出す（update.bat 等）
    - ユーザーに渡す手順書は極力短く、1〜2アクションに絞る
    - 複数の選択肢や詳細オプションを並べて「選んで」と投げるのは避ける
- 開発経験レベル: 不明（Unity / C# / VR 開発は未経験の可能性あり、優しく説明する）

## 技術スタック

- **エンジン**: Unity（2022.3 LTS または Unity 6 LTS 推奨）
- **VR SDK**: OpenXR + XR Interaction Toolkit（Valve Index Controller Profile 必須）
- **言語**: C#
- **キャラ素材候補**: VRoid Studio（アニメ調・無料）、Mixamo（リアル調・無料）、Character Creator 4（リアル調・有料）、Booth（VRChat アバター・要ライセンス確認）
- **ドキュメントサイト**: MkDocs + Material テーマ（`gh-pages` ブランチで公開）

## フォルダ構成

```
VRBoxing/
├── CLAUDE.md                ← このファイル
├── README.md                ← GitHub リポジトリの表紙（簡潔）
├── mkdocs.yml               ← MkDocs 設定
├── update.bat               ← 更新自動化スクリプト
├── .gitignore               ← Git 除外設定
├── docs/                    ← ドキュメントサイトの MD 元ファイル
│   ├── index.md             ← サイトのホーム（ダッシュボード）
│   ├── tasks.md             ← アクティブタスク管理
│   ├── steps/               ← 開発ステップ別計画
│   ├── design/              ← 設計メモ、開発ログ
│   ├── tech/                ← 技術・環境構築
│   ├── assets/              ← 素材管理ルール
│   └── misc/                ← ビルド、参考資料、バックアップ
├── 02_Assets_Source/        ← 素材の元データ（Unity外保管、実データ）
├── 03_Unity_Project/        ← Unity プロジェクト本体
├── 04_Builds/               ← ビルド済み実行ファイル
├── 05_Reference/            ← 参考資料（他人のもの）
└── 06_Backups/              ← 定期バックアップ
```

## ドキュメント運用

- **MD で書く、MkDocs で公開する**構成
- 新しい設定メモを作ったら **`docs/` 下に MD を追加** + **`mkdocs.yml` の `nav:` に1行足す**
- 更新は **`update.bat` をダブルクリック**するだけ（コミット → push → サイトデプロイ）
- 公開サイト: <https://annachloe2025.github.io/VRBoxing/>
- GitHub: <https://github.com/annachloe2025/VRBoxing>

## 開発ロードマップ

1. **ステップ1**: 殴れる女性サンドバッグ（2週間目安）← 現在ここ
2. **ステップ2**: 軽く動く／殴り返す相手（1〜2ヶ月）
3. **ステップ3**: 見た目・演出の作り込み
4. **ステップ4**: 本格的な対戦AI（将来）

詳細は `docs/steps/step1.md` 以降を参照。

## 現在のステータス

- **フェーズ**: ステップ1 / ボクシング実装フェーズ
- **最終更新**: 2026-04-23
- **直近の動き**: VRoid で作った自作女性キャラを UniVRM で Unity に取り込み、**VR 内で目の前に立たせることに成功** 🎯🎉
- **次のアクション**: パンチ判定の実装（両手コントローラーに拳コライダー、キャラにコライダー、速度しきい値でヒット判定、リアクション）

最新の詳細状況は `docs/tasks.md` と `docs/design/dev-log.md` を参照。

## 会話のルール（Claude 向け）

- **セッション開始時**: まずこのファイル → `docs/tasks.md` → `docs/design/dev-log.md` の最新エントリの順で読む
- **キャラ選定の相談**: 必ず女性キャラ前提で進める。男性キャラは提案しない
- **認知負荷を最優先**: ユーザーにやらせる作業を最小化する。提案よりも代替実行する
- **技術説明**: Unity / VR 初心者向けの丁寧さで始めて、理解度に応じて調整する
- **優先事項**: 「続けられること」＞「完成度」。モチベが下がる提案（何ヶ月も地味作業、など）は避け、早く動くものを作る方向に寄せる
- **ドキュメント更新**: 作業した内容は `docs/design/dev-log.md` に追記。完了タスクは `docs/tasks.md` を更新
- **ファイル作成時**: 計画（docs）→ 実装、の順。大きい変更の前にバックアップを勧める

## してはいけないこと

- Quest 向けの提案（ユーザーは持っていない）
- 「女性キャラが少ないのは Thrill of the Fight 2 で解決するかも」という理由で開発を止める提案（自前で作ることに意義がある）
- 過度にゴア・暴力的な表現の提案（フィットネス目的で続けるのに逆効果）
- チーム開発前提の提案（個人開発プロジェクト）
- **ユーザーに大量の作業ステップを投げること**（認知負荷で作業不能になる）

## 参照すべき主要ドキュメント

- `README.md` — プロジェクト入口（GitHub表紙）
- `docs/index.md` — サイトホーム（ダッシュボード）
- `docs/tasks.md` — 現在のタスク
- `docs/steps/step1.md` — ステップ1の詳細タスク
- `docs/design/notes.md` — ゲーム設計の考え方
- `docs/design/dev-log.md` — 日々の開発ログ
